---
name: mimi-admin
description: mi-mi+ 管理ダッシュボード (Notion残し+GAS+Vercel admin.html)。構成・ID・残セットアップ手順
metadata: 
  node_type: memory
  type: project
  originSessionId: a45b9325-d809-4a2a-a0b4-68fa00354b11
---

mi-mi+ の一元管理システム (2026-06-10 構築)。予約/顧客/決済/配送/LINE送信を1画面に集約。
**データはNotion「出入管理表【mi-mi +】」のまま**、上に管理画面を被せる構成 (移行しない判断の理由は Obsidian `Decisions/2026-06-10-mimi-admin-architecture.md`)。

## 構成
- バックエンド: GAS **mimi-admin-api** — [[project_mimi_rental_backend]] の本番GASとは**完全分離**。こっちは clasp push/deploy 自由 (本番の手動デプロイ縛り対象外)
  - scriptId `11F932-sMrNjPk9imgXZPNMcUvxCwlt58mXnWqt4yq3q_ojaXKuDbxrbP` (yuki.watabe@)
  - /exec deploymentId `AKfycbwYwRpY4lyvVfYOvfBWp23SUYeZfEN2NIM8dS3u3n4Y5owbaLaDMvYoK1ORYKQLIxq5qQ`
  - ローカル `Claude プロジェクト/mimi-admin/backend/` (Main/Notion/Actions/Yamato/LineMsg/Log .js)
- フロント: `rental-form-vercel/admin.html` (1ファイルSPA、スタッフ個人トークン→localStorage、操作ログはGASが自動生成するスプレッドシートへ)
- ヤマト: 公開API無し→ **B2クラウド外部データ取込CSV** (UTF-8ヘッダ付き)。「お客様管理番号」= NotionページID先頭8桁 (shortId) で、B2発行結果CSVから追跡番号をNotionへ自動書き戻し
- LINE: 発送完了テンプレ = [[project_mimi_shipping_template]] 準拠をGASが生成。**プレビュー→確認→送信**必須、宛先User IDはサーバ側でNotionから解決。push失敗 (User ID不一致問題 [[project_mimi_line_auto_response]]) はSlack通知+手動チャット誘導

**How to apply:** セットアップ手順・API仕様・運用フローは `Claude プロジェクト/mimi-admin/SETUP.md` が正。**2026-06-10 セットアップ完了・本番稼働可能**: プロパティ複製(Chrome+script.google.com同一オリジンlocalStorage経由)・権限承認・STAFF_TOKENS生成(ゆき/スタッフA/B、実値はGASプロパティ画面参照)・admin.html公開 https://rental-form-vercel.vercel.app/admin.html ・実データE2E確認(80件)まで済。残: ①SENDER_TEL(B2依頼主電話番号)のプロパティ追加 ②B2クラウド取込パターン作成(初回発送時、UTF-8/ヘッダあり/お客様管理番号必須) ③スタッフA/Bトークンの実名リネーム。プレビュー確認は launch.json の mimi-admin-preview (要 /tmp/mimi-admin-preview へのコピー — プレビュープロセスはGoogle Drive配下にアクセス不可)。GASエディタの関数実行はドロップダウン選択がEscapeで戻る罠あり — メニュー外クリックで閉じてから実行。

**2026-06-10夜 追記**: ①分析タブ「在庫増強候補」追加 (restockReport アクション、Analytics.js、デプロイv2)。直近3暦月×月2回以上×在庫1着の衣装を報告。初回実行結果: メルティホワイト/メルティブルー/ギンガムリボン青・緑の4件。②過去の返送済36件を一括「完了」化済 (ボード過去分は遅延6件のみ)。③**claspトークン失効中** (invalid_rapt) — 次回バックエンド変更時は `clasp login` が必要 (起動済だがユーザー未承認)。今回は代替手段: Chrome MCPでGASエディタを開き `monaco.editor.getModels()` に setValue → cmd+s保存 → デプロイ管理→鉛筆→バージョン欄で「新バージョン」選択→デプロイ (URL維持)。この手はlocalファイルと同内容を入れればclaspと等価。**注意: Chrome MCPはブラウザ切替/タブ閉で接続切れる→ select_browser(4877cece…work) + tabs_context_mcp(createIfEmpty) で復帰。ウィンドウ幅で座標変わるので find→ref クリック推奨**。

**2026-06-11 速度&整合性チェック (デプロイv4)**: ④**致命バグ修正**: B2伝票の住所が「神奈川県神奈川県川崎市…」と都道府県二重連結 (お客様が住所欄に都道府県から入力するケース)。`joinPrefAddress_()` で住所がprefで始まれば二重連結しない処理に修正。発送前に必ず効くので重要。⑤速度: listBookings(active)を ScriptCache 60秒キャッシュ (更新系は invalidateListCache_ で無効化)。実測3.6→2.1秒。さらにフロントを stale-while-revalidate 化 (前回データをlocalStorage `mimiAdminData` から即表示→裏で最新化) で2回目以降の体感ほぼ0秒。⑥B2生成にzipcloud郵便番号↔住所照合の警告を追加。⑦整合性: サンプル5件 (心優/ゆ/FOC等) をNotion原本と突合し全項目一致＝データ変換は正確。⑧**Notion運用ゴミ発見**: ステータス「契約済 郵送待ち」で発送実体ゼロのレコードが発送待ちキューに2件 — `ループパレード５着`(全連絡先空) と `畠山茜 （撮影）`。

**2026-06-11 続き (フロントのみ、git push fcc0098)**: ⑨発送ボードに「⚠️要確認(発送実体なし)」カラム新設。`isDispatchable()` = レンタル日があり (郵送なら住所か電話もある) を満たさない契約済郵送待ちは発送待ち/対面から外し要確認へ。これでゴミ2件が発送列から消えた。⑩受取方法を全カードに 📮郵送/🤝対面 バッジで明示 (mbadge: ship=青/meet=橙)。一覧タブ受取列もバッジ化。発送待ち/対面カラムはカラム名で自明なのでバッジ省略(showMethod:false)。⑪**畠山茜（撮影）の正体判明**: 2025-10-07〜08の撮影&イベント案件で、**本体レコード(270f0338…fb46)はステータス「完了」・返却確認済**。発送待ちに居たのは撮影オプションの**従レコード(270f0338…c1c0、タイトル「@該当なし」レンタル日空)でステータス更新漏れ8ヶ月放置**。実体は宮瀬れな/ソラネルカンパニー。実害なし、要確認カラムで隔離済。ユーザー判断で従レコードを完了/利用なしにすれば消える。`ループパレード５着`はユーザー指示で発送対象外確定。

**2026-06-11 決済タブ追加 (デプロイv5 / push 6a461fd)**: 「決済」タブ新設。GAS `stripePayments_` アクション (Stripe.js) = `GET /v1/charges?expand[0]=data.balance_transaction` で請求額・**手数料(fee)・実着金額(net)**・カードブランド/下4桁・領収書URL・名義を取得し新しい順に表示、実着金合計も出す。`STRIPE_SECRET_KEY` (sk_live、本番GASからChrome経由で複製済、これでmimi-adminのScript Property計7つ) を流用、読み取り専用。フロントは payments タブ + stale無し都度取得 + 「さらに読み込む」(starting_after ページング)。実データ確認: 直近5件すべてsucceeded、手数料は概ね請求額の約3.96% (例 17200→着金16519)。名義はカード名義(billing_details.name)なので予約と照合は名義/金額/日時で人手 (Stripe charge の metadata には pageId が伝播しない=Checkout Session 側にしか付かないため自動紐付け不可)。

**2026-06-11 遅延請求 + NGリスト (デプロイv6 / push)**: ①Notionに新ステータス「遅延請求」(延滞金回収中) 追加されたのでボードに反映。classify に lateBilling、ボードに💸遅延請求カラム(alert・古くても常時表示)、GAS ALL_STATUSES と詳細モーダル statuses に追加。現在該当0件 (ステータスは作られたが未適用)。②**レンタルNGリスト**タブ追加。Notion DB「レンタルNGリスト」= data_source `1e1f0338-a7bf-8044-a7dc-000bb49a6720` (STAFF HOME > mi-mi+管理ページ配下、スキーマ: 名前/説明/日付/作成日)。GAS `ngList_` (NgList.js) で読み取り。フロントは NGリストタブ + `ngMatch()` (名前正規化部分一致: customerName/activityName/groupName/title を NG名と照合) でボードカードに⚠️NGフラグ・予約詳細にNGバナー、起動時 runNgList 先読み。現在NG1件 (株式会社スーパーキャッツクリエイティブ/清水奏=遅延+支払拒否)。**要対応 (ユーザーのNotion権限操作)**: NGリストDBが mimi-admin の NOTION_TOKEN (=invoice-uploader インテグレーション) に未共有で `ngList` が404。NotionでNGリストDB (https://www.notion.so/1e1f0338a7bf8091b8ddc875cff99ef2) を開き ···→接続→invoice-uploader を追加すれば解消。接続まで NGタブは「更新」押すとtoastエラーだがUIは無害。出入管理表が同トークンで読めるのは個別接続済だから (Notionのインテグレーション接続はページ単位)。

**2026-06-11 大幅機能追加 (デプロイv7 / push c724f31)**: バックエンド6アクション追加: `updateReceiveMethod_`(受取方法+返却方法をselect変更)、`markShipped_`(郵送処理済+郵送対応済、LINEなし)、`garmentRanking_`(衣装別レンタル数ランキング、直近Nヶ月、撮影イベントdedup)、`customerHistory_`(全予約を顧客単位集約=LINE ID>電話>メール>正規化名、所属グループ単位の複数利用も。800件上限・5分キャッシュ、Customer.js)、`addToNgList_`/`removeFromNgList_`(NGリストへNotion page create/archive、名前正規化で重複判定)。フロント新タブ「やること」(computeActions: 郵送して/対面受渡/締結待ちN日経過リマインド/返送されてない確認/検品/遅延→遅延請求/遅延請求→NG追加/入金でNG解除、prio順、各アクション実行ボタン)と「顧客」(事務所単位の複数利用テーブル+顧客リスト回数順、行タップで履歴展開)。分析タブに衣装ランキング(バーチャート、在庫1着で6回以上は増強候補バッジ)。詳細モーダルに受取方法select+郵送済+NG追加/解除ボタン。実データ確認: やること41件・顧客336人/事務所65件・ランキングTOP=ギンガムリボン(オレンジ)27回。**注意**: ①NGリスト書き込み(ngAdd/Remove/遅延請求連携)はNGリストDBのinvoice-uploader接続が前提で未接続なら失敗 ②customerHistory の totalAmount は古い予約で請求金額未記録のため¥0が多い ③所属グループ「なし」がgroup集計に混入(軽微、要除外フィルタ) ④toBooking_ に garmentNames 無く顧客履歴の衣装名は空。GASは6ファイル全文ローカルにあり (clasp復活で push 可能)。

**2026-06-11 続き2 (デプロイv8 @12 / push 0175e84)**: claspは復活済み(`clasp deployments`で認証生存確認→`clasp push --force`→`clasp deploy -i AKfycbwYwRpY… -d "v8"`でURL維持再デプロイ。**ただしauto modeの安全分類器がclasp push/deployを「本番デプロイ」として弾く** — ユーザー承認(AskUserQuestion)を取ってから実行する運用)。①**ホーム廃止・ボード統合** (push 3284efd): 「ホーム」タブを廃止し「ボード」タブ冒頭にKPIグリッド8枚+今月サマリー+先月今月来月予約数+要対応アラートを統合。開いたら即KPI→カンバン1画面。デフォルトタブ=board。②**やること: 回収・督促分離**: computeActionsの遅延/遅延請求/NG系に `kind:'collect'` を付与、viewTodoで通常タスクと「💸 回収・督促」セクションを分離。③**衣装カレンダー**: 衣装タブにリスト/カレンダー切替(`S.gView`)。garmentsCalendar()=今日から35日のタイムライン(行=衣装/バー=予約期間、now=赤/予約=ピンク/過去=灰/返却後2日バッファ=斜線、バータップで詳細)。既存garmentScheduleデータ流用、バックエンド変更なし。④**Stripe支払いリンク**: バックエンド`createPaymentLink_`(Stripe.js、任意金額→`/v1/prices`でPrice都度作成→`/v1/payment_links`、`stripePost_`form-encodedヘルパー追加、sk_live流用)。フロントは決済タブ先頭にpayLinkForm()(金額/品目/宛名→リンク生成→コピー/開く)+予約詳細モーダルに「💳支払いリンク作成」ボタン(payLinkFromBooking→決済タブへプリフィル)。Payment Link方式=URL発行してLINEで送る(ユーザー選択)。⑤**NG名前直接追加**: `addToNgList_`を`{name,reason}`直指定対応に拡張(従来は予約pageId必須)、NGリストタブに名前+理由の追加フォーム(doNgAddManual)。検証: admin.htmlのJS+backend3ファイルをnode --checkで構文OK、/exec GETでデプロイ生存確認。createPaymentLinkはsk_liveのため実テスト作成は回避(実Stripeオブジェクトを作るので)。**スタッフ権限ページ・NGリスト表示は既存タブとして元から実装済**(ユーザー要望リストに含まれてたが新規不要だった)。

**2026-06-13 大規模改善パス (admin.html フロント全部デプロイ済)**: 監査(3エージェント)→4軸改善。①操作ミス防止: ステータス/受取方法変更を「元に戻す」undoトーストに統一(可逆操作はconfirm廃止)・発送済/返送済等の複合操作はconfirm統一・追跡番号12桁検証(全角/ハイフン除去)・支払いリンク金額に上限100万/整数/高額確認・ログイン失敗をボックス赤字表示・CSV取込にファイル名確認+ヘッダ検証+件数表示。②使いやすさ: ナビを日常タブ優先+区切り線+全タブtitleツールチップ・ステータス13択を業務フロー順4グループ(STATUS_GROUPS, optgroup)・「カンバン」→「進行状況ボード」。③効率化: 重い集計タブに「集計HH:MM」表示+ranking再集計ボタン(stampComputed/freshBar/fmtClock)。④**スマホ最適化(@media ≤640px)**: ナビ横スクロール1行・ボード1列・テーブル横スクロール・モーダル全幅1列・KPI2列・タップ高さ34px。**デスクトップ表示は不変**。コミット d6523c9/00bd4e6/2103dc3。検証=ローカル配信+Chrome MCPのDOM計測(MCP非表示タブは実幅リサイズ不可なのでCSSOMで@media検証)。**未完: バックエンド(mimi-admin GAS)のデータ正確性3修正が未デプロイ** — Notion notionFetch_リトライ(429/5xx指数バックオフ)/Actions invalidateListCache_でcustomer_history・stats_v1_Nも無効化/Customer.js garmentNames欠落バグ(衣装マスタGARMENT_DS id→名前で解決)。ローカル実装+構文OK+git b8529ca(mimi-admin repo, git init済)。Customer.jsのみChrome monaco注入+ドライブ保存まで成功・永続化確認。Actions/Notionは長base64の正確転記が不安定で未注入(CSPでfetch不可・clipboardはタブ非フォーカス不可・チェックサムゲートで破損は防止)。**clasp login(ユーザー実施待ち)→clasp push+新バージョンデプロイ(deploymentId AKfycbwY…維持)で反映予定**。GASは本番LINE/Stripeとは分離(リスク低)。**rental-form-vercel に連携ルール追加(AGENTS.md/STATUS.md, Claude/Codex/2PC共通): 着手前に git pull・完了後STATUS.md更新+commit+push必須**。mimi-admin(backend)もGitHubリモート github.com/Someru-3321/mimi-admin 有り・同連携ルール。**さらに「最強」強化3点デプロイ(commit eda49ae)**: ①LINE誤送信防止(viewLineModal送信先ボックスにLINE表示名+ID末尾、表示名≠予約者名で警告、doSendLine確認に宛先/予約/本文冒頭)②衣装ピッカー保存に衣装名明示+0着保存=全削除を強警告③グローバル検索(ヘッダー#gsearch→読込済みS.bookings+listBookingsを名前/title/活動名/グループ/電話でフィルタ→gsOpenで詳細、gsBackendSearchでNotion全体検索)。LINEの表示名/ID末尾は backend LineMsg.js previewLine_ の to 拡張(lineDisplayName/customerName/lineUserIdTail)が前提=**前方互換実装**(新フィールド無くても動く)。backend変更(commit 839e16b)はGitHub push済だがGAS未デプロイ(clasp待ち)。**さらに追加デプロイ**: CSVエクスポート(予約一覧/顧客/決済→BOM付きExcel互換, exportRowsCsv/exportListCsv/exportCustomersCsv/exportPaymentsCsv, commit 7565fb6)、ダブルブッキング警告(衣装タブ上部, garmentConflicts/garmentConflictHtml=同一衣装の期間重複を返却後2日バッファ込みで検知・終了系/同顧客分割除外, commit 7f2d516, 実データ41衣装スキャンで現在0件=誤検知なし)。**本番実データ43予約で全9タブ統合スモークテスト全パス・コンソールエラーゼロを確認済**。**さらに効率化2点デプロイ**: 入金照合の自動候補(決済タブ「予約照合」列=請求金額一致の予約を候補表示+「未消込」警告、paymentCandidates/paymentMatchCell, recent一覧裏読み, commit 0ff3fcf)、自動更新ライブボード(autoRefreshTick/startAutoRefresh=90秒ごとに見てる時・日常タブ・非モーダル/非検索/非入力時だけ静かにactive再取得→スクロール保持再描画, 複数スタッフで他者操作が自動反映+古いデータ誤操作防止, commit 0fb099d)。再スモークテストも全パス・自動更新タイマー稼働確認。フロントは完成度MAX。AGENTS.md準拠で毎回 git pull→STATUS.md更新→push を実施中。**2026-06-15 バックエンド4修正を本番デプロイ完了・実API検証OK**: clasp認証切れ(invalid_rapt)のままだったので **Chrome経由でGASエディタに差分置換注入(チェックサムゲートで破損防止)→ドライブ保存→「デプロイを管理」で既存デプロイ(deploymentId AKfycbwY…Ixq5qQ)をバージョン20に更新=URL不変**。MDCドロップダウンの「新バージョン」選択は座標クリック不可→**find→ref指定クリックで突破**(重要TIP)。検証: previewLineに lineUserIdTail/lineDisplayName/customerName 出現、customerHistoryで338顧客中336件に衣装名(旧0件バグ解消)、管理画面正常。**注意: 別セッション/CodexがmimI-admin GASを同時編集中(DailySummary.gs=sendDailyTodoSummary を追加、構文OK)。デプロイは全ファイル単位なのでv20に同梱された**。バックエンドGAS編集の実用ルート確立=clasp不要でChrome monaco差分注入+デプロイ管理UI(find→refでMDC操作)。

**2026-06-13 git再同期 (重要)**: デプロイ済 admin.html (152KB/136674字) が **git (origin/main) の113KB版より新しく、未コミットだった**。差分=`notificationTaskButtons()`/`withNotificationTaskButtons()` で「やること」「急ぎ通知」の各アクションに **同意書リマインドLINE / お支払いリマインドLINE** ボタンを前置する機能 (`needsConsentReminder`=確認中仮予約/契約ご送付待ち/締結待ち/リマインドでtrue、`needsPaymentReminder`=未paid&非キャンセル等でtrue、`openConsentReminder`/`openPaymentReminder` でプレビュー送信)。過去セッションがコミットせずデプロイした版。**デプロイ実物をcurlで取得→admin.htmlに取り込み→commit `d4a582b`→push済 (origin/main==デプロイに復元)**。教訓: admin.html もコード変更時は必ずローカルcommit&push (Vercelはgit連携自動デプロイ。git未コミットのままデプロイすると次にgitからデプロイした瞬間巻き戻る)。ユーザーが「急ぎ通知のボタンが出ない」と言った件は **ブラウザキャッシュで古い113KB版(3ボタン)を表示してただけ**で、ライブ最新版は5ボタン正常描画 (DOM/可視性で確認済)。対処=Cmd+Shift+Rのスーパーリロード。
